---
description: Pre-flight review of local unpushed commits before pushing — squash planning, commit-message validation, and a pre-push validation checklist.
---

Load the `git-push-planner` skill and follow its workflow end-to-end: inspect the unpushed commits, identify redundant or similar commits that should be squashed, validate each commit message against PPR and GitHub conventions, and produce a pre-push validation checklist. Do not push anything yourself — the output is a validated plan. $ARGUMENTS