---
description: Run execution-focused validation for the current working tree.
---

Load the `validation` skill for the current working tree.

{user's optional scope: presets, targets, files, or requested checks}

The validation owner selects proportionate configure, build, test, and
inspection checks, records actual execution outcomes, and incorporates the
`code-reviewer` verdict. Do not restate review dimensions in this wrapper.

