# Required: resolve each image to an immutable digest before cutover.
# Accepted form: registry/namespace/image:reviewed-tag@sha256:<64 lowercase hex>
CRAWL4AI_IMAGE=
SEARXNG_IMAGE=
VALKEY_IMAGE=

# Required secrets. Generate externally and install locally; never commit .env.
CRAWL4AI_API_TOKEN=
SECRET_KEY=
SEARXNG_SECRET=
